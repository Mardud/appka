<h1>Editing Task</h1>

<%= form_for :task, url: day_task_path(@task.day, @task), method: :patch do |f| %>
    <% if @task.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@task.errors.count, "error") %> prohibited
            this post from being saved:</h2>
          <ul>
            <% @task.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= f.label :t_name %><br>
      <%= f.text_field :t_name %>
    </p>
    <p>
      <%= f.label "Done" %> <br />
      <%= f.check_box :done %> <br />
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>
